const {test}=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'../src/renderer');
test('voice input button is beside send and uses native streaming recognition without model configuration',()=>{const html=fs.readFileSync(path.join(root,'index.html'),'utf8'),app=fs.readFileSync(path.join(root,'app.js'),'utf8'),css=fs.readFileSync(path.join(root,'workbench.css'),'utf8');assert.ok(html.indexOf('id="voiceBtn"')<html.indexOf('id="sendBtn"'));assert.match(html,/voice-icon/);assert.match(app,/SpeechRecognition \|\| window\.webkitSpeechRecognition/);assert.match(app,/continuous = true/);assert.match(app,/interimResults = true/);assert.match(app,/onresult/);assert.match(app,/isFinal/);assert.match(app,/recognition\.stop/);assert.match(app,/recognition\.abort/);assert.match(app,/voiceState === 'stopping'/);assert.match(app,/stopTimer = setTimeout/);assert.match(css,/voice-level/);assert.match(css,/is-listening/);});
test('voice input preserves existing draft before final and interim text',()=>{const app=fs.readFileSync(path.join(root,'app.js'),'utf8');assert.match(app,/baseText = chatInput\.value\.trimEnd\(\)/);assert.match(app,/baseText\}\$\{interimText/);assert.match(app,/chatInput\.dispatchEvent\(new Event\('input'/);});
