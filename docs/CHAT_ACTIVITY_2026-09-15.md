# 聊天工具与思考模块调整 — 2026-09-15

状态：阶段进度，非最终交付。依据用户在本轮提供的桌面照片调整视觉结构；照片未建立目标构建号或动画时长基线。

## 已实现
- 工具从默认隐藏的卡片改为默认可见、无边框的紧凑行。
- 使用真实工具参数展示操作、文件名、目录或命令；只有成功结果携带的变更数据才展示增删行数。未执行/被拒绝不冒充完成。
- 原生按钮展开参数与输出，保留文件差异详情。DOM 控件 ID 独立于模型调用 ID，避免历史重用 ID 导致点开另一条记录。
- 思考模块默认为折叠文本行；内容、强度、token 信息仍可展开。时长仅由实际接收阶段计时累积，旧历史缺少时长时留空。
- 等待提示改为 `IEXA正在思考…` 单一文字扫光；球体、胶囊边框、背景与阴影均已从此模块移除。
- 前一轮的三档动效设置保留；Windows 减少动态效果环境下可在“外观 → 完整动效”启用扫光。

## 证据
- E1：实现 `src/renderer/services/ChatActivityView.js`、`src/renderer/app.js`、`src/renderer/workbench.css`。
- E2：`npm test`（含构建），退出码 0，54/54 通过：`.iexa-artifacts/chat-activity-20260915/tests.log`。
- E3：`scripts/check-chat-activity.cjs` 隐藏离屏 Chromium 验证 4 组布局（深色、浅色、窄窗口、手机宽度），检查紧凑行、展开、无横向溢出，退出码 0；截图及 `visual-checks.json` 位于同目录。
- E4：`scripts/check-motion.cjs` 验证 12 组主题/系统/应用偏好，确认文字渐变随时间移动、无等待球体、透明背景、减少模式静态、发送图标居中，退出码 0；记录位于同目录 `motion/`。

截图是 IEXA 合成数据测试夹具，用来验证真实渲染代码；不是执行了截图中的工具，也不是 Codex 真实运行截图。捕获前等待字体、主题切换和绘制帧稳定，避免把主题过渡中间帧当作最终效果。

## 剩余工作
- 当前传输与历史结构仍将多段思考、正文、工具分别聚合。参考照片中完整交错的事件时间线及其历史恢复尚待统一事件记录；本轮不宣称这部分一致。
- 桌面参照构建、精确动效测量、全量真实模型端到端以及安装包验收继续保留未决状态。
- 用户正在使用的生产窗口未被本轮强制重启。

## 工件与回滚
`.iexa-artifacts/chat-activity-20260915/`：`baseline.json` / `original/`、`changes.json`、`changes.diff`、测试日志、截图与验证记录。

`rollback.py` 默认只校验；传 `--apply` 恢复本轮原件。检测到后续编辑时终止，避免覆盖新工作；新增文件会移入本工件目录保留，不执行递归删除。
